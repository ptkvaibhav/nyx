

export function explainPricingStrategy() {
  return [
    "Keep provider plans in a refreshable catalog with an explicit as-of date.",
    "Do not hardcode plan prices into routing logic.",
    "Use pricing only for advisory recommendations, never for upload execution."
  ];
}

